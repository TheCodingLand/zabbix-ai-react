
//THIS FILE IS AUTO GENERATED BY BUILD SCRIPT FROM config.js.example, which replaces all the caps variables to the value of environment variables.
//this allows to configure the app easily from environment variables at build time

const config = { https: HTTPS, apiRoot : "DOMAIN", loginRequired : LOGINREQUIRED }
export default config
